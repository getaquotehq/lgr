name = "qlhq-api-proxy"
main = "src/index.ts"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]

# Custom domain — configure in Cloudflare Dashboard → Workers → Triggers → Custom Domains
# after deploying. Point api.leadgenrentalshq.com to this worker.

[vars]
SUPABASE_FUNCTION_URL = "__LGR_HQ_SUPABASE_URL__/functions/v1/api"
